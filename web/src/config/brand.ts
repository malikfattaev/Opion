/**
 * Фирменный стиль в одном месте.
 *
 * Логотип лежит файлом в `public/brand`, а не зашит в разметку: заменить его
 * можно, положив новый файл поверх старого. Если пропорции изменятся —
 * поправьте `width` и `height`: по ним браузер резервирует место под картинку
 * ещё до загрузки и не дёргает вёрстку.
 *
 * Источник: media/brand/logo/logo horizontal.PNG
 */
export const brand = {
  logo: {
    src: "/brand/logo.png",
    width: 479,
    height: 192,
    /** Высота логотипа в шапке, px. Ширина считается по пропорции. */
    headerHeight: 26,
  },
  /** Цвета логотипа. Те же значения продублированы токенами в globals.css. */
  colors: {
    cream: "#F8F1E7",
    black: "#000000",
  },
} as const;
